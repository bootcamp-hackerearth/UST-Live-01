@model HealthAxis.Shared.Dtos.DoctorDto

<style>
    .page-container {
        max-width: 600px;
        margin: 40px auto;
    }

    .card {
        background: #1f2937;
        padding: 30px;
        border-radius: 16px;
        color: #e5e7eb;
    }

    h2 {
        margin-bottom: 20px;
    }

    .form-wrapper {
        max-width: 450px;
    }

    .form-group {
        margin-bottom: 15px;
    }

    input, select {
        width: 100%;
        padding: 12px;
        border-radius: 10px;
        border: none;
        background: #374151;
        color: white;
        font-size: 14px;
    }

        input:focus, select:focus {
            outline: none;
            background: #4b5563;
        }

    .validation {
        color: #ef4444;
        font-size: 13px;
    }

    .btn-group {
        display: flex;
        justify-content: space-between;
        margin-top: 25px;
    }

    .btn-save {
        background: #10b981;
        padding: 10px 20px;
        border-radius: 10px;
        border: none;
        color: white;
        cursor: pointer;
    }

    .btn-back {
        background: #374151;
        padding: 10px 20px;
        border-radius: 10px;
        text-decoration: none;
        color: white;
    }

        .btn-back:hover {
            background: #4b5563;
        }
</style>

<div class="page-container">

    <div class="card">

        <h2>Add Doctor</h2>

        @if (ViewBag.Error != null)
        {
            <div class="validation">
                @ViewBag.Error
            </div>
        }

        <div class="form-wrapper">

            @using (Html.BeginForm())
            {
                @Html.ValidationSummary(true, "", new { @class = "validation" })

                <div class="form-group">
                    @Html.TextBoxFor(m => m.FullName, new { placeholder = "Full Name" })
                    @Html.ValidationMessageFor(m => m.FullName, "", new { @class = "validation" })
                </div>

                <div class="form-group">
                    @Html.DropDownListFor(
                        m => m.Specialisation,
                        new SelectList(Enum.GetValues(typeof(HealthAxis.Shared.Dtos.Specialisation))),
                        "Select Specialisation"
                    )
                    @Html.ValidationMessageFor(m => m.Specialisation, "", new { @class = "validation" })
                </div>

                <div class="form-group">
                    @Html.TextBoxFor(m => m.YearsOfExperience,
                        new { type = "number", placeholder = "Years of Experience" })
                    @Html.ValidationMessageFor(m => m.YearsOfExperience, "", new { @class = "validation" })
                </div>

                <div class="form-group">
                    @Html.TextBoxFor(m => m.ConsultationFee,
                        new { type = "number", placeholder = "Consultation Fee" })
                    @Html.ValidationMessageFor(m => m.ConsultationFee, "", new { @class = "validation" })
                </div>

                <div class="btn-group">
                    <a href="@Url.Action("Index", "Doctor")" class="btn-back">
                        ← Back
                    </a>

                    <button type="submit" class="btn-save">
                        Add Doctor
                    </button>
                </div>
            }

        </div>

    </div>

</div>