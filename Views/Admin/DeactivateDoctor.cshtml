@model HealthcareMvcApp.Models.Doctor

@{
    ViewBag.Title = "Deactivate Doctor";
}

<h2 class="text-danger">Do you wish to deactivate this doctor?</h2>

<div class="col-5">
    <table class="table table-striped">
        <tbody>
            <tr>
                <td>Doctor ID</td>
                <td>@Model.DoctorId</td>
            </tr>
            <tr>
                <td>Name</td>
                <td>@Model.FullName</td>
            </tr>
            <tr>
                <td>Specialisation</td>
                <td>@Model.Specialisation</td>
            </tr>
            <tr>
                <td>Practice Start Date</td>
                <td>@Model.PracticeStartDate.ToString("yyyy-MM-dd")</td>
            </tr>
            <tr>
                <td>Years of Experience</td>
                <td>@Model.YearsOfExperience</td>
            </tr>
            <tr>
                <td>Consultation Fee</td>
                <td>@Model.ConsultationFee</td>
            </tr>
            <tr>
                <td>Active</td>
                <td>@Model.IsActive</td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex">
        @using (Html.BeginForm("DeactivateDoctor", "Admin", new { id = Model.DoctorId }, FormMethod.Post))
        {
            <button type="submit" class="btn btn-danger me-2">Deactivate</button>
        }

        @Html.ActionLink("Cancel", "Doctors", "Admin", null, new { @class = "btn btn-secondary" })
    </div>
</div>