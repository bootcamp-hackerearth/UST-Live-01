export const environment = {
  production: false,

  // ASP.NET Core Web API URL
  apiBaseUrl: '',

  // Blazor Admin app URL, fallback if backend does not send redirectUrl
  blazorAdminUrl: '/blazor'
};
